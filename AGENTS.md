# Nagoya 2026 itinerary editing rules

이 저장소의 일정 페이지를 수정할 때는 아래 규칙을 반드시 지킨다.

## 1. 수정 방식
- 화면에 보이는 한두 항목만 땜질하지 않는다.
- 수정 전 `index.html`의 실제 렌더링 내용을 확인한다.
- 같은 장소/같은 의미의 일정이 기존에 있는지 먼저 찾고, `추가`가 아니라 `대체/병합/삭제` 여부를 결정한다.
- 런타임 문자열 치환으로 일정을 바꾸지 않는다.
- `index.html`을 단일 소스로 관리하고, 동일 정보를 여러 군데 중복으로 하드코딩하지 않는다.
- 일정 수정은 부분 패치보다 Day 단위 canonical 목록 재구성을 우선한다.

## 2. 일정 데이터 원칙
- 사용자가 제공한 실제 일정/장소를 기준으로 한다. 임의의 장소나 동선을 만들어 넣지 않는다.
- 같은 장소는 같은 Day 안에서 의미 없이 두 번 나오면 안 된다.
- 같은 의미의 항목은 하나로 병합한다. 예: `Urban Quar Spa & Living`은 별도 새 일정이 아니라 기존 `대욕장` 일정과 매칭한다.
- 장소와 기능을 구분한다. 예: KITTE Nagoya는 Day 3에서 별도 관광지가 아니라 초밥집 위치 맥락으로 사용한다.
- 이동/식사/쇼핑/관광/대욕장 등 기존 역할을 최대한 유지하면서 실제 장소명으로 치환한다.

## 3. 공유용 문구 원칙
- 페이지는 다른 사람에게 공유하는 완성본이다.
- `트리플에서 저장`, `사용자가 정리`, `원본 자료`, `내부 메모`, `임시`, `테스트` 같은 작업 과정/출처 표현을 노출하지 않는다.
- 문구는 여행자 관점에서 자연스럽게 작성한다.
- 일본어/영문 장소는 필요 시 `원문 (한국어)` 형식으로 한 번만 표기한다. 괄호 번역이 중복되지 않게 한다.

## 4. 매 수정 후 필수 전체 검수
부분 수정만 확인하고 끝내지 않는다. 아래 항목을 전체 페이지 기준으로 모두 점검한다.

### A. 중복 검사
- Day 1~4 각각 모든 시간표 항목을 훑는다.
- 같은 장소명이 두 번 이상 등장하는지 확인한다.
- 같은 장소가 표기만 달라 중복된 경우도 확인한다. 예: `오아시스21` / `Oasis 21`, `JOYLAB` / `조이랩`.
- 상단 요약, 시간표, 상세 동선, 끼니 계획 등 여러 섹션에서 의도치 않은 중복/충돌이 없는지 확인한다.

### B. 순서 검사
- 각 Day가 사용자가 제공한 실제 방문 순서와 맞는지 확인한다.
- 시간 역전, 이동 불가능한 순서, 병합 후 남은 옛 슬롯이 없는지 확인한다.

### C. 의미 매칭 검사
- 새 장소가 기존 일정의 어떤 역할을 대체하는지 확인한다.
- 같은 의미를 새 항목으로 추가해 중복시키지 않는다.
- 대표 매칭 예시:
  - Urban Quar Spa & Living = 대욕장
  - 아츠타 호라이켄 마츠자카야점 = 히츠마부시 점심
  - KITTE Nagoya = Day 3 초밥집 위치 맥락

### D. 표현 검사
- 일본어/영문 한글 괄호 표기가 한 번만 붙는지 확인한다.
- 공유용 페이지에 내부 작업 문구가 없는지 확인한다.
- 장소명이 사용자 제공 자료와 다르게 임의 변형되지 않았는지 확인한다.

### E. 렌더링 검사
- 최종 HTML 구조상 숨겨진 기존 슬롯이 남아 있지 않은지 확인한다.
- JS 치환 없이 `index.html` 자체가 최종 일정으로 렌더링되는지 확인한다.
- 모바일 화면에서 같은 일정이 두 줄/두 카드로 중복 노출될 가능성을 확인한다.

## 5. 자동 검증은 필수
- 모든 일정 수정 후 반드시 `node scripts/validate-itinerary.js`가 통과해야 한다.
- GitHub Actions의 `Validate itinerary` 워크플로가 push/PR마다 실행된다.
- 검증 스크립트가 실패하면 일정 수정은 미완료 상태다.
- 검증 스크립트를 우회하거나 삭제해서 통과시키지 않는다. 실제 데이터/렌더 구조를 수정한다.
- `index-base.html` 같은 두 번째 일정 원본을 만들지 않는다.
- 이미 발생했던 회귀 항목(JOYLAB, 오아시스21, 선샤인 사카에/Sky-Boat, 히츠마부시/아츠타 호라이켄, Urban Quar, KITTE)은 고정 회귀 검사 대상으로 유지한다.
- 새로운 중복 사고가 발견되면 해당 케이스를 검증 스크립트에 회귀 테스트로 추가한다.

## 6. 완료 기준
다음 조건을 모두 만족하기 전에는 사용자에게 `수정 완료`라고 말하지 않는다.
1. 요청한 변경이 반영됨.
2. 해당 Day 전체를 다시 검수함.
3. Day 1~4 전체에서 새 변경으로 인한 중복/충돌이 없는지 확인함.
4. 공유용 문구 검사 완료.
5. 실제 렌더링 구조에서 옛 항목이 남지 않았음을 확인함.
6. `node scripts/validate-itinerary.js` 통과.
7. GitHub Actions `Validate itinerary` 결과 확인.

## 7. 특히 금지
- 스크린샷에서 잘 안 보이는 장소명을 추측해 넣기.
- 사용자가 제공하지 않은 추천 동선을 임의로 추가하기.
- 한 항목을 고친 뒤 같은 장소가 다른 슬롯에 남아 있는지 확인하지 않고 종료하기.
- `아마`, `보일 것`, `이제 될 것` 상태에서 완료 처리하기.
- 자동 검증을 실행/확인하지 않고 완료 처리하기.

이 규칙은 이후 모든 일정 수정에 우선 적용한다.
